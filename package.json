{
  "name": "schema-org-gen",
  "version": "0.0.0",
  "description": "Generate JSON Schemas from schema.org data",
  "main": "generate.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "repository": "",
  "author": "Geraint Luff",
  "dependencies": {
    "request": "~2.27.0"
  },
  "private": true
}
